# Traefik ingress overlay.
# The ingress class is kept blank in values.yaml (never hardcoded) and set here;
# the request body size is capped by a traefik Middleware (replacing the previous
# proxy-body-size ingress annotation). Include this file in real-environment deploys.
app:
  api:
    ingress:
      className: traefik
      annotations:
        traefik.ingress.kubernetes.io/router.middlewares: "{{ $.Release.Namespace }}-ercs-body-size@kubernetescrd"
  extraManifests:
    ercs-body-size:
      apiVersion: traefik.io/v1alpha1
      kind: Middleware
      metadata:
        name: ercs-body-size
      spec:
        buffering:
          maxRequestBodyBytes: 20971520  # 20Mi (previously proxy-body-size: 20m)
