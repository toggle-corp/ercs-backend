app:
  environment: ALPHA

  # NOTE: Postgres is provisioned externally and Redis by the
  # Dragonfly operator (./values/operators.yaml); S3/media wiring, if needed,
  # is injected at deploy time. Only env tweaks live here.
  env:
    SENTRY_TRACES_SAMPLE_RATE: "0.8"
    SENTRY_PROFILE_SAMPLE_RATE: "0.8"
